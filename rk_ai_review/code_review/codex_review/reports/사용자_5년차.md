# 사용자 5년차 코드 리뷰 — CBRD-26246

> **2026-08-24 전수 재감사 알림:** 아래 8개 항목은 최초 탐색 원문이므로 삭제하지 않았다. 다만 일부 설명은 E2E 검증 뒤 정정됐다. CR-02는 master 무로그, CR-05는 입증 보류, CR-08·17은 오분석, CR-12는 develop 기존/feature 악화, CR-13은 선행 결함 의존이며 CR-16은 사용자 가정에 따라 제외한다. 현재 판정은 [187개 재감사 매핑](RAW_187_REAUDIT_MAP.md)과 [E2E 작업표](E2E_FINDING_WORKSHEETS.md)를 우선한다. 이 페르소나는 최대 50건까지 재탐색했으나 8개에서 근거 있는 관찰이 소진되어 **더 이상 발견되지 않음**으로 종료했다.

> 비채택 후보도 삭제하지 않으며, 상태와 판정 근거는 [후보 보존표](CANDIDATE_AUDIT.md#비채택-후보-보존표)에 기록한다.

## 결과

공식 `/home/youngjun/Workspace/claude` HEAD `9e094324b`를 `upstream/develop` `5b9c0d815`와 대조해 실제 관찰 가능한 SQL·utility 경로만 남겼다. **유효 finding 8건**을 찾았고 표현만 다른 항목은 합쳤다. **더 이상 근거 있는 문제를 찾지 못함**.

## USER5-00 로그 파일을 만들 수 없으면 rkcheck가 정상 오류 대신 죽을 수 있다

- 심각도/신뢰도/관점: High / High / 사용자
- feature 근거/경로: `src/executables/util_cs.c:2855-2864`은 결과 파일 `fopen` 실패 시 NULL을 반환하지만 `3320-3343`은 이를 검사하지 않고 출력 macro에 넘긴다. `cubrid heartbeat start`가 내부 rkcheck를 호출한다.
- develop 비교/PR: rkcheck feature/#6658 신규이며 #7370 review에서 지적된 뒤 최신 HEAD에도 남아 있다.
- 재현/불변식: log directory에 쓸 수 없게 한 뒤 heartbeat start. 사용자는 crash가 아니라 원인과 실패 exit status를 받아야 한다.
- 수정·테스트: 파일 open 실패를 명확한 메시지로 반환하고 권한 부족/디스크 부족 테스트를 추가한다.

## USER5-01 생성은 성공하지만 복제되지 않는 복합 UNIQUE 테이블

- 심각도/신뢰도/관점: Critical / High / 사용자
- feature 근거/경로: `src/object/class_object.c:95-98` → `src/object/schema_manager.c:16115-16129`는 복합 UK 중 하나만 NN이어도 `check_ha_repl_constraint`를 통과시킨다. 실제 DML은 `src/base/object_representation_sr.c:4712-4719`에서 전부 NN일 때만 로그 후보로 쓴다.
- develop 비교/PR: feature 신규, #6477의 detection fix가 불완전.
- 재현/불변식: `CREATE TABLE t(a int NOT NULL,b int,UNIQUE(a,b));` 후 HA DML. 성공한 REPLICATION=ON 테이블은 쓰기가 replica에 반영돼야 한다.
- 수정·테스트: 동일 all-NN 규칙을 SQL validation에도 적용하고 오류가 CREATE/ALTER 시점에 나게 한다.

## USER5-02 멀티 ALTER 순서에 따라 RK 제거 성공 여부가 달라진다

- 심각도/신뢰도/관점: High / High / 사용자
- feature 근거/경로: `src/query/execute_schema.c:2051`이 현재 절이 아닌 첫 `alter` code를 검사한다. `do_alter`에서 모든 절을 실행한 뒤 조건부로만 RK 확인한다.
- develop 비교/PR: #6618/#6637이 추가한 feature 로직의 신규 오류.
- 재현/불변식: 같은 두 절을 `RENAME, DROP CONSTRAINT rk`와 역순으로 실행해 결과 비교. 순서가 안전성 검사를 우회하면 안 된다.
- 수정·테스트: `alter_code` 사용 및 순열 회귀 테스트.

## USER5-03 NOT NULL 제거가 허용된 뒤 DML 시점에 복제가 깨진다

- 심각도/신뢰도/관점: High / High / 사용자
- feature 근거/경로: 관련 ALTER macro `src/query/execute_schema.c:109-114`에 CHANGE/MODIFY가 없고 실제 handler는 `2013-2016`. 변경 후 `check_ha_repl_constraint`가 호출되지 않는다.
- develop 비교/PR: feature 신규 검증 누락; #6739 이후에도 잔존.
- 재현/불변식: 유일 RK의 NN 제거 후 INSERT/UPDATE. invalid state는 ALTER에서 거부돼야 한다.
- 수정·테스트: 적격성 변경 ALTER 전체를 후검사한다.

## USER5-04 같은 스키마처럼 보여도 index 생성 순서에 따라 다른 RK가 사용된다

- 심각도/신뢰도/관점: High / High / 사용자
- feature 근거/경로: `src/storage/btree.c:8285-8298`은 이름이나 PK 우선순위 없이 첫 후보를 선택하고, source insert/delete도 `src/transaction/locator_sr.c:8039-8055`에서 처음 만난 후보를 사용한다. 가정된 UPDATE helper 수정은 이 선택 규칙을 바꾸지 않는다.
- develop 비교/PR: develop PK 고정 대비 feature에서 신규.
- 재현/불변식: PK/NN UK 또는 NN UK 두 개의 생성 순서를 바꿔 dump/load한 뒤 동일 update/delete. schema 의미가 같으면 복제 결과도 같아야 한다.
- 수정·테스트: 선택 RK를 `SHOW CREATE` 가능한 명시 metadata로 고정한다.

## USER5-05 HA start가 실패했는데 server는 떠 있을 수 있다

- 심각도/신뢰도/관점: Medium / High / 사용자
- feature 근거/경로: `src/executables/util_service.c:3963-3973`은 server start 후 rkcheck 실패 시 반환하며 stop이 없다.
- develop 비교/PR: #6658 신규 부분 실패.
- 재현/불변식: 제약 위반 상태에서 heartbeat start의 실패 메시지 뒤 process 상태 확인. 명령 결과와 실제 상태가 일치해야 한다.
- 수정·테스트: rollback 및 명확한 메시지/exit code 테스트.

## USER5-06 OFF 테이블을 읽는 USE_SBR 결과가 replica에서 달라진다

- 심각도/신뢰도/관점: High / High / 사용자
- feature 근거/경로: `src/query/execute_statement.c:3315-3343`은 수정 target만 ON인지 검사하고 INSERT...SELECT/join source는 보지 않는다. `3405-3407`은 SBR 선택 시 row replication을 suppress한다.
- develop 비교/PR: #6908에서 명시된 known limitation이며 REPLICATION=OFF 도입으로 현실화.
- 재현/불변식: OFF source에 master-only row를 넣고 ON target에 USE_SBR `INSERT SELECT`. 양 노드 결과가 같아야 한다.
- 수정·테스트: OFF source SBR 차단 또는 RBR fallback과 사용자 오류 안내.

## USER5-07 partition promote 후 일부 UNIQUE가 표시/동작에서 사라질 수 있다

- 심각도/신뢰도/관점: High / High / 사용자
- feature 근거/경로: `src/query/execute_schema.c:7943-7956`은 첫 NN UNIQUE만 attribute flag를 남기면서 `7976-7983`은 property를 통째로 보존한다.
- develop 비교/PR: #6552/#7370 신규 변경.
- 재현/불변식: NN UNIQUE 두 개가 있는 partition을 promote한 뒤 `SHOW CREATE`와 constraint 조회, duplicate INSERT를 비교한다.
- 수정·테스트: 모든 보존 대상 constraint를 일관되게 재구성하고 unload/load round trip도 검증한다.
