# 응용 개발자 1년차 코드 리뷰

> **2026-08-24 전수 재감사 알림:** 아래 8개 항목은 최초 탐색 원문이므로 삭제하지 않았다. 다만 일부 설명은 E2E 검증 뒤 정정됐다. CR-02는 master 무로그, CR-05는 입증 보류, CR-08·17은 오분석, CR-12는 develop 기존/feature 악화, CR-13은 선행 결함 의존이며 CR-16은 사용자 가정에 따라 제외한다. 현재 판정은 [187개 재감사 매핑](RAW_187_REAUDIT_MAP.md)과 [E2E 작업표](E2E_FINDING_WORKSHEETS.md)를 우선한다. 이 페르소나는 최대 50건까지 재탐색했으나 8개에서 근거 있는 관찰이 소진되어 **더 이상 발견되지 않음**으로 종료했다.

> 비채택 후보도 삭제하지 않으며, 상태와 판정 근거는 [후보 보존표](CANDIDATE_AUDIT.md#비채택-후보-보존표)에 기록한다.

## 요약

공식 최신 `/home/youngjun/Workspace/claude@9e094324b`와 `upstream/develop@5b9c0d815`의 schema migration 및 DML/API 흐름을 caller/callee로 검토했다. 채택 finding은 **8건**이다. 50건을 억지로 채우지 않았고 **더 이상 근거 있는 문제를 찾지 못함**. #7697에서 해결된 interrupt/error propagation 및 per-index lookup 성능 후보는 삭제하지 않고 `후속 PR 해결` 상태로 보존했다.

## APP1-01 ORM이 만드는 복합 UNIQUE가 DDL과 DML에서 서로 다르게 판정된다

- 심각도/신뢰도: **Critical / High**; 영향: 응용/마이그레이션 개발자
- feature 근거/경로: `class_object.c:95-98` → `schema_manager.c:16115-16129`는 ANY NOT NULL, `object_representation_sr.c:4712-4719`는 ALL NOT NULL. CREATE는 `execute_schema.c:10267-10273`, DML은 `locator_sr.c:8041-8049,8421-8426`.
- develop 비교: UNIQUE RK 지원으로 신규.
- PR 이력: #6477/#6739/#7370.
- 재현/불변식: nullable natural-key 일부를 포함한 ORM unique constraint로 migration 후 CRUD. migration 성공 스키마는 CRUD 복제가 가능해야 한다.
- 수정·테스트: 공통 predicate와 migration→CRUD integration test.

## APP1-02 여러 natural key 중 선택된 RK가 schema metadata에 고정되지 않는다

- 심각도/신뢰도: **Critical / High**
- feature 근거/경로: source array 첫 후보 `locator_sr.c:8039-8049,8418-8426`; apply local 첫 후보 `btree.c:8285-8298` → `locator_sr.c:6848-6856`; TODO `object_representation_sr.c:4692`.
- develop 비교: PK-only에서 다중 후보로 확장해 신규.
- PR 이력: #7370/CBRD-26096.
- 재현/불변식: migration tool이 constraint drop/recreate로 순서를 바꾼 노드에서 apply.
- 수정·테스트: RK identity를 schema/API에 노출하고 로그에 전달.

## APP1-03 partial/function unique index를 RK로 오인한다

- 심각도/신뢰도: **High / High**
- feature 근거/경로: `class_object.c:95-98`, `object_representation_sr.c:4702-4721`은 filter/function 속성을 검사하지 않는다. filter false는 `locator_sr.c:7863-7876`에서 replication block 전 continue.
- develop 비교: feature 신규.
- PR 이력: #7370.
- 재현/불변식: soft-delete용 partial unique index가 유일한 key인 ON table에서 predicate 밖 CRUD.
- 수정·테스트: partial/function index 제외 및 ORM migration fixture.

## APP1-04 multi-clause migration은 첫 ALTER 절의 종류에 따라 검사가 빠진다

- 심각도/신뢰도: **High / High**
- feature 근거/경로: `execute_schema.c:1851-1855`의 current code 대신 `2051-2054`가 head code를 사용; 검사 `2059-2072`.
- develop 비교: 신규 RK validation 우회.
- PR 이력: #6618/#6637/#6739.
- 재현/불변식: migration generator가 comment/change를 먼저 두고 RK constraint를 뒤에서 제거.
- 수정·테스트: `alter_code` 사용 및 migration clause permutation.

## APP1-05 mixed-table SBR이 REPLICATION=OFF 경계를 보존하지 않는다

- 심각도/신뢰도: **High / Medium**
- feature 근거/경로: `execute_statement.c:3320-3343` ANY ON → `3404-3419,4112-4125` whole-statement SBR.
- develop 비교: class별 flag/SBR feature 신규 조합.
- PR 이력: #6467/#6908.
- 재현/불변식: batch/ORM multi-target UPDATE가 audit OFF table과 business ON table을 함께 수정.
- 수정·테스트: mixed target statement 거부 또는 split; trigger 포함 테스트.

## APP1-06 OFF 테이블의 TRUNCATE 기반 test cleanup이 replica도 지울 수 있다

- 심각도/신뢰도: **Medium / High**
- feature 근거/경로: `execute_statement.c:361-383`은 RK 존재만 확인; `16673-16682`가 SBR.
- develop 비교: OFF 의미 도입 뒤 gate 누락.
- PR 이력: #6826/#6908.
- 재현/불변식: OFF staging table을 TRUNCATE하는 배치 실행 후 replica 데이터 확인.
- 수정·테스트: replication class flag check.

## APP1-07 HA start 실패가 자동화에서 깨끗한 rollback을 의미하지 않는다

- 심각도/신뢰도: **High / High**
- feature 근거/경로: `util_service.c:3963-3973` server start 후 check; `4001-4008` rollback 없음.
- develop 비교: feature 신규 failure mode.
- PR 이력: #6658/#6934.
- 재현/불변식: CI/provisioning이 nonzero 반환 후 재시도할 때 기존 server가 남아 포트/process 충돌.
- 수정·테스트: transactional start 또는 idempotent cleanup/retry test.

## APP1-08 rkcheck output open 실패가 자동화 가능한 오류로 반환되지 않는다

- 심각도/신뢰도: **High / High**; 영향: 배포/마이그레이션 자동화 개발자
- feature 근거/경로: `util_cs.c:2856-2864`의 nullable `fopen` 결과가 `3320-3334,3346-3349`에서 무검사 stream으로 사용된다.
- develop 비교: feature 신규 rkcheck 경로.
- PR 이력: #6658/#6934/#7370 inline review 미해결.
- 재현/불변식: read-only log volume에서 provisioning이 rkcheck를 실행한다. process signal이 아니라 안정된 exit/error로 실패해야 한다.
- 수정·테스트: open error 즉시 반환, errno 포함, EACCES/ENOSPC automation test.

## 결론

응용 개발자가 생성하는 일반적인 migration/partial index/batch SQL이 곧바로 문제 경로에 들어가므로 API 문서 보완만으로 해결되지 않고 코드 판정 통합이 필요하다.
