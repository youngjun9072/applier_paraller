# 응용 개발자 5년차 코드 리뷰 — CBRD-26246

> **2026-08-24 전수 재감사 알림:** 아래 8개 항목은 최초 탐색 원문이므로 삭제하지 않았다. 다만 일부 설명은 E2E 검증 뒤 정정됐다. CR-02는 master 무로그, CR-05는 입증 보류, CR-08·17은 오분석, CR-12는 develop 기존/feature 악화, CR-13은 선행 결함 의존이며 CR-16은 사용자 가정에 따라 제외한다. 현재 판정은 [187개 재감사 매핑](RAW_187_REAUDIT_MAP.md)과 [E2E 작업표](E2E_FINDING_WORKSHEETS.md)를 우선한다. 이 페르소나는 최대 50건까지 재탐색했으나 8개에서 근거 있는 관찰이 소진되어 **더 이상 발견되지 않음**으로 종료했다.

> 비채택 후보도 삭제하지 않으며, 상태와 판정 근거는 [후보 보존표](CANDIDATE_AUDIT.md#비채택-후보-보존표)에 기록한다.

## 결과

공식 `/home/youngjun/Workspace/claude` HEAD `9e094324b`와 `upstream/develop` `5b9c0d815`를 schema migration, DML, metadata·utility 중심으로 비교했다. **유효 finding 8건**이며 **더 이상 근거 있는 문제를 찾지 못함**.

## APP5-00 자동화된 HA 사전 검사가 결과 파일 open 실패에서 crash할 수 있다

- 심각도/신뢰도/관점: High / High / 응용 개발자
- 근거·경로: `src/executables/util_cs.c:2855-2864`은 결과 파일 `fopen` 결과를 반환하고 `rkcheck:3320`은 NULL check 없이 `3332-3343`의 출력 macro를 호출한다. 배포 script의 `heartbeat start`도 같은 경로다.
- develop 비교/PR: feature/#6658 신규. #7370 review에서 확인됐고 HEAD에도 잔존한다.
- 재현/불변식: 컨테이너에서 read-only log volume 또는 quota/FD fault로 rkcheck 실행. automation은 signal crash가 아닌 안정된 exit code를 받아야 한다.
- 수정·테스트: open error를 utility error로 전파하고 cleanup, errno 메시지, shell exit-code 회귀 테스트를 추가한다.

## APP5-01 ORM이 만든 복합 UNIQUE가 잘못 RK로 승인된다

- 심각도/신뢰도/관점: Critical / High / 응용 개발자
- 근거·경로: `src/object/class_object.c:95-98`의 client predicate가 `src/object/schema_manager.c:16115-16129`의 any-NN helper를 사용하나 server predicate `src/base/object_representation_sr.c:4712-4719`는 all-NN이다. DDL 성공 후 `locator_*` DML log는 생성되지 않는다.
- develop 비교/PR: feature 신규; #6477 후속 판정 fix 불완전.
- 재현/불변식: migration으로 `(tenant_id NOT NULL, external_id NULL)` UNIQUE 생성 후 DML. 승인된 ON table의 쓰기는 복제돼야 한다.
- 수정·테스트: 공통 predicate 및 ORM형 composite schema matrix.

## APP5-02 migration의 멀티 ALTER에서 뒤 절의 RK drop이 누락된다

- 심각도/신뢰도/관점: High / High / 응용 개발자
- 근거·경로: `src/query/execute_schema.c:2051-2054`가 loop-local `alter_code` 대신 첫 node를 검사한다.
- develop 비교/PR: #6618/#6637 feature 신규.
- 재현/불변식: rename/comment를 첫 절, RK drop을 둘째 절로 둔 migration. 순서와 무관하게 최종 schema validation이 같아야 한다.
- 수정·테스트: local code 사용, 다절 순열 integration test.

## APP5-03 column nullability migration이 RK validation을 우회한다

- 심각도/신뢰도/관점: High / High / 응용 개발자
- 근거·경로: `src/query/execute_schema.c:109-114` 관련 목록에서 `PT_CHANGE_ATTR`/`PT_MODIFY_ATTR_MTHD` 누락; handler `2013-2016` 후 최종 검사 없음.
- develop 비교/PR: feature 신규.
- 재현/불변식: NN UNIQUE column을 nullable로 바꾸는 migration 후 DML.
- 수정·테스트: constraint 적격성 변경을 결과 schema 기준으로 검사.

## APP5-04 선택 RK가 metadata API에 노출되지 않아 migration 안정성을 검증할 수 없다

- 심각도/신뢰도/관점: Medium / High / 응용 개발자
- 근거·경로: 선택은 `src/storage/btree.c:8285-8298`의 첫 index라는 런타임 순서뿐이다. `src/object/schema_system_catalog_install_query_spec.cpp:68-75`와 `src/object/object_printer.cpp:1137-1145`는 ON/OFF flag만 노출하고 선택 constraint는 내보내지 않는다.
- develop 비교/PR: develop은 PK라는 명시 metadata가 곧 key였으나 feature는 복수 UK 후보를 도입해 신규 모호성이 생겼다.
- 재현/불변식: PK+UK schema를 dump/load 또는 migration 재생 후 실제 RK가 같은지 API로 검증할 수 없다.
- 수정·테스트: catalog/JDBC metadata에 RK constraint name을 제공하고 round-trip 테스트.

## APP5-05 OFF source를 참조한 SBR DML은 replica에서 다른 rowset을 만든다

- 심각도/신뢰도/관점: Critical / High / 응용 개발자
- 근거·경로: `src/query/execute_statement.c:3315-3343`은 modify target만 확인하고 source table 상태는 무시한다. `3405-3407`에서 SBR을 선택하면 RBR이 suppress된다.
- develop 비교/PR: #6908 known limitation + feature OFF table로 신규 현실화.
- 재현/불변식: `INSERT INTO on_t SELECT ... FROM off_t` 또는 join update에 USE_SBR. 같은 statement가 replica의 다른 source를 읽어선 안 된다.
- 수정·테스트: dependency set에 OFF class가 있으면 RBR/거부; prepared/non-prepared 모두 테스트.

## APP5-06 derived vclass walker가 수정 대상이 아닌 내부 ON class만 보고 SBR을 켤 수 있다

- 심각도/신뢰도/관점: Medium / Medium / 응용 개발자
- 근거·경로: `src/query/execute_statement.c:3239-3263` callback은 derived subtree의 모든 `PT_SPEC` 중 ON class 하나만 찾으면 true이고 target 기여 여부를 구분하지 않는다. `3294-3300`이 이를 target 판정에 사용한다.
- develop 비교/PR: #6908의 derived-vclass 후속 코드에서 신규.
- 재현/불변식: updatable vclass rewrite subtree에 OFF modify base와 ON join-only base가 함께 있는 형태에서 USE_SBR 여부를 확인한다.
- 수정·테스트: update mapping에 실제 쓰이는 base spec만 추적하고 vclass join/derived test를 추가한다.

## APP5-08 partition promote가 여러 UNIQUE constraint의 flag/property를 다르게 보존한다

- 심각도/신뢰도/관점: High / High / 응용 개발자
- 근거·경로: `src/query/execute_schema.c:7943-7956`은 첫 qualifying attr만 flags 보존, `7976-7983`은 class property 전체 보존.
- develop 비교/PR: #6552/#7370 신규 회귀.
- 재현/불변식: multiple NN UK schema를 partition promote 후 schema introspection과 duplicate insert 비교.
- 수정·테스트: constraint 단위 복사/삭제와 dump-load migration 회귀 테스트.
